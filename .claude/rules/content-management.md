# Content Management

## Courses

At the moment all content is managed by writing markdown files, which are translated
into JSON through a script.

Hence, every time a new course is asked for, a new markdown file should be created in the `src/content/courses` directory.