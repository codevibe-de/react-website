import {TextBlock} from "@/types/TextBlock";

export type IllustratedTextContent = {
    headLine: string;
    introText: string;
    subHeadLine: string;
    mainText: TextBlock[];
    imageUrls: string[];
};